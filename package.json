{
  "name": "wenyan-rev",
  "private": true,
  "version": "2.5.3",
  "type": "module",
  "scripts": {
    "copy-readme": "node scripts/copy-welcome.js",
    "tauri-dev": "npm run copy-readme && tauri dev",
    "tauri": "npm run copy-readme && tauri build"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@tauri-apps/cli": "^1.6.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0",
    "tauri-plugin-clipboard-api": "^1.1.4"
  }
}
